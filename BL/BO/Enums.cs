namespace BO;

public enum Category
{
    Suit,
    Pants,
    Shirts,
    Ties,
    Cufflinks
}

public enum OrderStatus
{
    CONFIRMED,
    SENT,
    DELIVERED
}

public enum StartChoose
{
    EXIT,
    CART,
    PRODUCT,
    ORDER
}

